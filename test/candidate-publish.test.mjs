import test from "node:test";
import assert from "node:assert/strict";
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { SwarmRouter } from "../src/router.mjs";
const config = (root) => ({ repository: root, runtimeDir: join(root, "runtime"), baseRef: "main", project: { name: "test", documentationDir: "docs/in", generatedDir: "docs/out" }, router: { maxConcurrentTasks: 1, maxChildrenPerTask: 1, maxDelegationDepth: 1, maxPlanTasks: 1, defaultParentBudget: 100, turnTimeoutMs: 1000, approvalMode: "deny" }, autonomy: { mode: "autonomous", autoPush: true, autoCreatePullRequest: true, autoMerge: true }, budget: { weeklyTokenLimit: 1000, weeklyWindowDays: 7 }, quota: { throttleAtUsedPercent: 90 }, delivery: {}, remote: { enabled: true, remoteName: "origin", allowedRemotes: ["origin"], candidateBranchPrefix: "swarm/candidate/", requireCi: false }, roles: Object.fromEntries(["bootstrap","planner","backend","frontend","database","qa","security","devops"].map((role) => [role, { sandbox: "read-only", approvalPolicy: "never", tokenBudget: 100, usesWorktree: false }])) });
test("direct publication cannot create completed_merged without the persisted delivery contract", async () => { const root = mkdtempSync(join(tmpdir(), "candidate-")); const router = new SwarmRouter(config(root)); try { const result = await router.publishCandidate({ path: "manifest.json", manifest: { id: "manifest", status: "candidate_ready", branch: "swarm/candidate/test", candidateSha: "a".repeat(40), localVerification: { status: "passed" } } }); assert.equal(result.terminalState, "conflict_blocked"); } finally { router.close(); rmSync(root, { recursive: true, force: true }); } });
